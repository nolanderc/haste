package main

import "fmt"

func main() {
    fmt.Printf("hello")
}

func blah(a string, b, c int) {
	for ; r != nil; r = r.next {
		if r.i != i {
			t.Fatalf("bad scan: expected %d got %d", i, r.i)
		}
		i++
	}
}

